import React, { useRef, useEffect } from "react";

import SearchComponent from "@search/SearchApp";

const SearchApp = () => (
  <div>
    oi
    <SearchComponent />
  </div>
);

export default SearchApp;
